Runned commands:

insmod memory.ko

rmmod memory


Observations:
    Allocated and initialized memory for struct task_info following the
    requirements from the lab, displayed the structures and freed the memory.